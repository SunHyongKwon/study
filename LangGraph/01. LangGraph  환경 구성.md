## 개발 환경 설정 가이드

### 1. 가상 환경 생성 및 의존성 설치

먼저 Python 3.11 버전의 Conda 가상 환경을 생성하고 필요한 패키지를 설치합니다.

```bash
# 가상 환경 생성
conda create -n langgraph_env python=3.11

# 가상 환경 활성화
conda activate langgraph_env

# 의존성 패키지 설치
pip install -r requirements.txt
```

### 2. Graphviz 설치 (Optional)

> **Note:** LangGraph의 그래프 시각화 기능을 사용하기 위한 필수 도구입니다.

| OS          | 설치 방법                                               |
| :---------- | :------------------------------------------------------ |
| **Windows** | [📥 설치 파일 다운로드](https://graphviz.org/download/) |
| **macOS**   | `brew install graphviz`                                 |
| **Linux**   | `sudo apt-get install graphviz`                         |
